import { MatchPasswordPatternDirective } from './match-password-pattern.directive';

describe('MatchPasswordPatternDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchPasswordPatternDirective();
    expect(directive).toBeTruthy();
  });
});
